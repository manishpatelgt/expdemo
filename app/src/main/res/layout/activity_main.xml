<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimaryDark"
        android:theme="@style/AppTheme.AppBarOverlay"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:popupTheme="@style/AppTheme.PopupOverlay" />

    <!-- 1st button Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/without_singleton_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/green_button_selector"
        android:fontFamily="@font/circular_black"
        android:text="@string/without_singleton"
        android:textColor="@color/white"
        android:textSize="@dimen/dimen_11sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar"  />

    <!-- 2nd button Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/with_singleton_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/green_button_selector"
        android:fontFamily="@font/circular_black"
        android:text="@string/with_singleton"
        android:textColor="@color/white"
        android:textSize="@dimen/dimen_11sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/without_singleton_button" />

    <!-- 3rd button Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/coroutine_test_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/green_button_selector"
        android:fontFamily="@font/circular_black"
        android:text="@string/coroutine_test"
        android:textColor="@color/white"
        android:textSize="@dimen/dimen_11sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/with_singleton_button" />

    <!-- 4th button Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/retrofit_generic_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/green_button_selector"
        android:fontFamily="@font/circular_black"
        android:text="@string/retrofit_generic"
        android:textColor="@color/white"
        android:textSize="@dimen/dimen_11sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/coroutine_test_button" />

    <!-- 5th button Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/fragment_factory_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/green_button_selector"
        android:fontFamily="@font/circular_black"
        android:text="@string/fragment_factory_demo"
        android:textColor="@color/white"
        android:textSize="@dimen/dimen_11sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/retrofit_generic_button" />

    <!-- 6th button Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/coroutine_test_2_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/green_button_selector"
        android:fontFamily="@font/circular_black"
        android:text="@string/coroutine_test2"
        android:textColor="@color/white"
        android:textSize="@dimen/dimen_11sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/fragment_factory_button" />

    <!-- 7th button Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/view_model_state_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/green_button_selector"
        android:fontFamily="@font/circular_black"
        android:text="@string/view_model_state_demo"
        android:textColor="@color/white"
        android:textSize="@dimen/dimen_11sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/coroutine_test_2_button" />

</androidx.constraintlayout.widget.ConstraintLayout>